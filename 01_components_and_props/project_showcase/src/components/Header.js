import React from 'react'

function Header() {
    return (
        <header>
            <h1>
                <span className="logo">{"//"}</span>Project showcase
            </h1>
        </header>
    )
}

export default Header;